--- libs/lz4-r123/cmake_unofficial/CMakeLists.txt	2014-10-31 16:38:11.346334129 -0300
+++ libs/lz4-r123/cmake_unofficial/CMakeLists.txt	2014-10-31 19:04:39.786517080 -0300
@@ -15,8 +15,8 @@
     MESSAGE( STATUS "64 bit architecture detected size of void * is " ${SIZEOF_VOID_P})
 ENDIF()
 
-option(BUILD_TOOLS "Build the command line tools" ON)
-option(BUILD_LIBS  "Build the libraries in addition to the tools" OFF)
+option(BUILD_TOOLS "Build the command line tools" OFF)
+option(BUILD_LIBS  "Build the libraries in addition to the tools" ON)
 
 if(UNIX AND BUILD_LIBS)
     if(CMAKE_SYSTEM_PROCESSOR MATCHES "x86_64")
@@ -27,7 +27,7 @@
 set(LZ4_DIR ../)
 set(PRG_DIR ../programs/)
 set(LZ4_SRCS_LIB ${LZ4_DIR}lz4.c ${LZ4_DIR}lz4hc.c ${LZ4_DIR}lz4.h ${LZ4_DIR}lz4hc.h)
-set(LZ4_SRCS ${PRG_DIR}xxhash.c ${PRG_DIR}bench.c ${PRG_DIR}lz4cli.c ${PRG_DIR}lz4io.c)
+set(LZ4_SRCS ${LZ4_DIR}xxhash.c ${PRG_DIR}bench.c ${PRG_DIR}lz4cli.c ${PRG_DIR}lz4io.c)
 
 if(BUILD_TOOLS AND NOT BUILD_LIBS)
     set(LZ4_SRCS ${LZ4_SRCS} ${LZ4_SRCS_LIB})
